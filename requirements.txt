Flask>=3.1,<4
flask-cors>=6.0,<7
gunicorn>=23.0,<24
geopandas>=1.1,<2
shapely>=2.1,<3
numpy>=2.5,<3
pandas>=3.0,<4
rasterio>=1.5,<2
scipy>=1.18,<2
scikit-image>=0.26,<0.27

# usados só pelo script offline de preparação de dados (backend/scripts/prepare_data.py),
# não são necessários em produção — mas mantidos aqui para simplicidade de deploy
requests>=2.34,<3
geovoronoi>=0.4,<0.5
